# Plymouth script for Omni-OS

# Deep Black Background
Window.SetBackgroundTopColor(0.02, 0.02, 0.02);
Window.SetBackgroundBottomColor(0.02, 0.02, 0.02);

# Omni-OS Uploaded Glowing Logo
logo.image = Image("logo.png");
logo.sprite = Sprite(logo.image);
logo.sprite.SetX(Window.GetWidth() / 2 - logo.image.GetWidth() / 2);
logo.sprite.SetY(Window.GetHeight() / 2 - logo.image.GetHeight() / 2);
logo.sprite.SetZ(100);

# Spinner text below logo
spinner_sprite = Sprite();
spinner_sprite.SetZ(100);
spinner_tick = 0;
spinner_index = 0;
spinner_texts[0] = ". o O o .";
spinner_texts[1] = "o . o O o";
spinner_texts[2] = "O o . o O";
spinner_texts[3] = "o O o . o";

# Rocket Streaks (perfectly streaked, white)
streak_image = Image.Text("|", 1.0, 1.0, 1.0, 0.8, "Sans 40");
NUM_STREAKS = 20;

i = 0;
while (i < NUM_STREAKS) {
    streaks[i].sprite = Sprite(streak_image);
    # Perfectly streaked horizontally spaced
    streaks[i].x = (Window.GetWidth() / NUM_STREAKS) * i;
    streaks[i].y = Math.Random() * Window.GetHeight();
    streaks[i].speed = 5 + (Math.Random() * 10);
    streaks[i].sprite.SetX(streaks[i].x);
    streaks[i].sprite.SetY(streaks[i].y);
    streaks[i].sprite.SetZ(1);
    i++;
}

fun refresh_callback () {
    # Animate streaks
    i = 0;
    while (i < NUM_STREAKS) {
        streaks[i].y += streaks[i].speed;
        if (streaks[i].y > Window.GetHeight()) {
            streaks[i].y = -100;
            # Keep the perfect horizontal spacing
            streaks[i].speed = 5 + (Math.Random() * 10);
        }
        streaks[i].sprite.SetY(streaks[i].y);
        streaks[i].sprite.SetX(streaks[i].x);
        i++;
    }
    
    # Animate spinner
    spinner_tick++;
    if (spinner_tick % 10 == 0) {
        spinner_index++;
        if (spinner_index > 3) {
            spinner_index = 0;
        }
        spinner_img = Image.Text(spinner_texts[spinner_index], 1.0, 1.0, 1.0, 1.0, "Sans 20");
        spinner_sprite.SetImage(spinner_img);
        spinner_sprite.SetX(Window.GetWidth() / 2 - spinner_img.GetWidth() / 2);
        # Position below the logo
        spinner_sprite.SetY(Window.GetHeight() / 2 + logo.image.GetHeight() / 2 + 40);
    }
}

Plymouth.SetRefreshFunction(refresh_callback);

# Hide default boot messages for clean cinematic look
fun display_message_callback (text) {
}
Plymouth.SetDisplayMessageFunction(display_message_callback);
Plymouth.SetUpdateStatusFunction(display_message_callback);

# LUKS / Disk Encryption Password Prompt
password_prompt_sprite = Sprite();
password_prompt_sprite.SetPosition(Window.GetWidth() / 2, Window.GetHeight() / 2 + 140, 200);

fun display_password_callback (prompt, bullets) {
    bullet_string = "";
    b = 0;
    while (b < bullets) {
        bullet_string += "●";
        b++;
    }
    text = prompt + " " + bullet_string;
    my_image = Image.Text(text, 0.0, 0.898, 1.0, 1.0, "Sans 16");
    password_prompt_sprite.SetImage(my_image);
    password_prompt_sprite.SetX(Window.GetWidth() / 2 - my_image.GetWidth() / 2);
}
Plymouth.SetDisplayPasswordFunction(display_password_callback);

fun display_normal_callback () {
    password_prompt_sprite.SetImage(Image());
}
Plymouth.SetDisplayNormalFunction(display_normal_callback);

# Fix bootloop by providing a Quit callback
fun quit_callback () {
}
Plymouth.SetQuitFunction(quit_callback);

